# Learnings — mesh-nav

## 2026-04-25 Session Start
- `.sisyphus/drafts/discovery-governance.md` does NOT exist on disk
- D-GOV1-D-GOV8 need to be created from plan context, not parsed from file
- Current discovery/ has 6 .md + 2 dirs (design/, research/) — no state/ or archive/ yet
- AGENTS.md is currently ~45 lines, well under 80 line cap
- Python scripts must use stdlib only — no pip deps
- SKILL.md must never contain raw SQL — calls Python scripts only
