Task 15: Implement migration steps 4-5 (import + verify)

=== CHANGES ===
Files modified:
- internal/body/migration.go
- internal/body/body_test.go

=== IMPLEMENTATION ===

Step 4 (import):
- stepImport() checks migration record CurrentStep >= 4 for idempotency
- Validates target handle exists (from step 2 provision)
- For local transfer, import is already done in step 3 (transfer), so step 4 is a no-op validation
- Fixed ResumeMigration to recover newHandle from body record InstanceID

Step 5 (verify):
- stepVerify() checks migration record CurrentStep >= 5 for idempotency
- Validates target handle exists
- Health check via adapter.GetStatus() — container must be Running or Created
- Exec verification command ("echo ok") — must exit 0
- Filesystem state check via "ls /" — must return non-empty output

=== TESTS ===
New tests added:
- TestMigrationStepImportRestoresFiles: verifies import step completes
- TestMigrationStepImportIdempotent: verifies no duplicate work on resume
- TestMigrationStepVerifyDetectsMissingFiles: verifies recovery after import failure
- TestMigrationStepVerifyDetectsUnhealthyContainer: verifies container health check

All existing tests still pass (including T14 tests).

=== TEST RESULTS ===
go test ./internal/body/ - PASS (28 tests)
go test ./... - PASS (all packages)
