// Package version provides the single source of truth for the Mesh version.
package version

// Version is the current release version of Mesh.
// Bump this when cutting a new release, then tag the commit with the same value.
const Version = "1.0.6"
