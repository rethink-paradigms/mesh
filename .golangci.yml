run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gosimple
    - ineffassign

issues:
  exclude-files:
    - ".*_test.go"
  exclude-dirs:
    - "vendor"
