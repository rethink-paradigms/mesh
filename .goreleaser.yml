version: 2

builds:
  - main: ./cmd/mesh/
    binary: mesh
    goos:
      - linux
    goarch:
      - amd64
    ldflags:
      - -s -w

archives:
  - format: binary

release:
  github:
    owner: rethink-paradigms
    name: mesh
